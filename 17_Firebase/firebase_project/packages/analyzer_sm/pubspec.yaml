name: analyzer_sm
description: Analyzer package for SM App

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: '>=2.19.6 <3.0.0'

dependencies:
  flutter:
    sdk: flutter
  pedantic_mono: ^1.21.1
  dart_code_metrics: ^5.7.2

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  #uses-material-design: true