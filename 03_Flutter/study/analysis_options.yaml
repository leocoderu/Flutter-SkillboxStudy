# This file configures the analyzer, which statically analyzes Dart code to
# check for errors, warnings, and lints.
#
# The issues identified by the analyzer are surfaced in the UI of Dart-enabled
# IDEs (https://dart.dev/tools#ides-and-editors). The analyzer can also be
# invoked from the command line by running `flutter analyze`.

# The following line activates a set of recommended lints for Flutter apps,
# packages, and plugins designed to encourage good coding practices.
#include: package:flutter_lints/flutter.yaml

#linter:
  # The lint rules applied to this project can be customized in the
  # section below to disable rules from the `package:flutter_lints/flutter.yaml`
  # included above or to enable additional rules. A list of all available lints
  # and their documentation is published at
  # https://dart-lang.github.io/linter/lints/index.html.
  #
  # Instead of disabling a lint rule for the entire project in the
  # section below, it can also be suppressed for a single line of code
  # or a specific dart file by using the `// ignore: name_of_lint` and
  # `// ignore_for_file: name_of_lint` syntax on the line or in the file
  # producing the lint.
#  rules:
    # avoid_print: false  # Uncomment to disable the `avoid_print` rule
    # prefer_single_quotes: true  # Uncomment to enable the `prefer_single_quotes` rule

# Additional information about this file can be found at
# https://dart.dev/guides/language/analysis-options

# PEDANTIC
#include: package:pedantic/analysis_options.yaml
#
#analyzer:
#  strong-mode:
#    implicit-casts: false
#    implicit-dynamic: false
#  #Исключение файлов из анализатора
#  exclude:
#    - lib/**/*.g.dart
#    - lib/views/**
#    - lib/component/modal.dart
#
#  # info / warning / error / ignore
#  errors:
#    implicit_dynamic_return: error
#    camel_case_types: error
#    await_only_futures: error
#    file_names: error
#    constant_identifier_names: error
#    avoid_function_literals_in_foreach_calls: error
#
#linter:
#  rules:
#    # Переменные должны быть в camelCase стиле
#    constant_identifier_names: false
#    # Имена файлов должны быть в нижнем регистре и разделены подчеркиванием lowercase_with_underscores
#    file_names: true
#    # Использовать цикл for in вместо forEach
#    avoid_function_literals_in_foreach_calls: true

## EFFECTIVE DART
#include: package:effective_dart/analysis_options.yaml
#
#analyzer:
#  strong-mode:
#    implicit-casts: false
#    implicit-dynamic: false
#  #Исключение файлов из анализатора
##  exclude:
##    - lib/**/*.g.dart
##    - lib/views/**
##    - lib/component/modal.dart
#
#  # info / warning / error / ignore
#  errors:
#    public_member_api_docs: ignore
#    implicit_dynamic_return: ignore
#    camel_case_types: warning
#    missing_return: error
#    dead_code: info
#    annotate_overrides: error
#    await_only_futures: error
#    cancel_subscriptions: error
#    close_sinks: error
#    comment_references: error
#    file_names: error
#    constant_identifier_names: error
#    control_flow_in_finally: error
#    empty_statements: error
#    avoid_function_literals_in_foreach_calls: error
#
#linter:
#  rules:
#    #
#    avoid_shadowing_type_parameters: false
#    #
#    await_only_futures: true
#    # Переменные должны быть в camelCase стиле
#    constant_identifier_names: false
#    # Имена файлов должны быть в нижнем регистре и разделены подчеркиванием lowercase_with_underscores
#    file_names: true
#    # Использовать цикл for in вместо forEach
#    avoid_function_literals_in_foreach_calls: true

# LINT
include: package:lint/analysis_options.yaml

analyzer:
  strong-mode:
    implicit-casts: false
    implicit-dynamic: false
  #Исключение файлов из анализатора
  exclude:
    - lib/**/*.g.dart
    - lib/views/**
    - lib/component/modal.dart

  # info / warning / error / ignore
#  errors:
#    public_member_api_docs: ignore
#    implicit_dynamic_return: ignore
#    camel_case_types: warning
#    missing_return: error
#    dead_code: info
#    annotate_overrides: error
#    await_only_futures: error
#    cancel_subscriptions: error
#    close_sinks: error
#    comment_references: error
#    file_names: error
#    constant_identifier_names: error
#    control_flow_in_finally: error
#    empty_statements: error
#    avoid_function_literals_in_foreach_calls: error
#
#linter:
#  rules:
#    #
#    avoid_shadowing_type_parameters: false
#    #
#    await_only_futures: true
#    # Переменные должны быть в camelCase стиле
#    constant_identifier_names: false
#    # Имена файлов должны быть в нижнем регистре и разделены подчеркиванием lowercase_with_underscores
#    file_names: true
#    # Использовать цикл for in вместо forEach
#    avoid_function_literals_in_foreach_calls: true